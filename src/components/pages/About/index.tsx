import React from 'react';
import Layout from '../../common/Layout';

const AboutPage: React.FC = () => (
  <Layout title="Temp title" description="Temp description">
    <div>about</div>
  </Layout>
);
export default AboutPage;
